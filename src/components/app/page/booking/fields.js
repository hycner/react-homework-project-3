// @flow
import React, {PureComponent} from 'react';
import styled from 'styled-components';

type TProps = {

};

const Wrap = styled.div`
  padding: 36px 61px;
`;

export default class Fields extends PureComponent<TProps> {
  render() {
    return (
      <Wrap>

      </Wrap>
    );
  }
}
